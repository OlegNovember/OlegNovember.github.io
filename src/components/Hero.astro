<section class="hero">
    <div class="hero-inner">
        <div class="hero-content">
            <div class="hero-badge reveal d-1">
                <span class="dot"></span> Свободен для новых проектов
            </div>
            <h1 class="hero-title reveal d-2" id="heroTitle" data-scramble="Лендинги, которые продают"></h1>
            <p class="hero-sub reveal d-3">
                Создаю страницы под ключ за 3 дня. Адаптив, SSL, форма заявки, поддержка. 
                От <b>9 000 ₽</b> — без скрытых платежей.
            </p>
            <div class="hero-buttons reveal d-4">
                <a href="#form" class="btn btn-primary">
                    <span>Заказать лендинг</span>
                    <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </a>
                <a href="#portfolio" class="btn btn-ghost">Смотреть работы</a>
            </div>
            <div class="hero-trust reveal d-5">
                <div class="trust-item"><b>50+</b><span>клиентов</span></div>
                <div class="trust-divider"></div>
                <div class="trust-item"><b>3 дня</b><span>срок</span></div>
                <div class="trust-divider"></div>
                <div class="trust-item"><b>100%</b><span>адаптив</span></div>
            </div>
        </div>
        <div class="hero-visual reveal-scale d-3">
            <div class="hero-card">
                <div class="hero-card-header">
                    <div class="dots"><span></span><span></span><span></span></div>
                    <div class="hero-card-url">olegnovember.github.io</div>
                </div>
                <div class="hero-card-body">
                    <div class="mock-line long"></div>
                    <div class="mock-line medium"></div>
                    <div class="mock-line short"></div>
                    <div class="mock-btn"></div>
                </div>
            </div>
            <div class="hero-float hero-float-1"><span>⚡</span> 3 дня</div>
            <div class="hero-float hero-float-2"><span>✓</span> Под ключ</div>
        </div>
    </div>
</section>

<style>
    .hero {
        padding: 180px 0 120px;
        position: relative;
        overflow: hidden;
        background: linear-gradient(-45deg, #0a0e14, #0f1a2e, #1a2332, #0a0e14);
        background-size: 400% 400%;
        animation: gradientShift 18s ease infinite;
    }
    @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    .hero::before {
        content: "";
        position: absolute;
        top: -20%; right: -10%;
        width: 700px; height: 700px;
        background: radial-gradient(circle, rgba(59,130,246,0.15), transparent 70%);
        border-radius: 50%;
        animation: float 12s ease-in-out infinite;
    }
    .hero::after {
        content: "";
        position: absolute;
        bottom: -30%; left: -10%;
        width: 500px; height: 500px;
        background: radial-gradient(circle, rgba(139,92,246,0.12), transparent 70%);
        border-radius: 50%;
        animation: float 15s ease-in-out infinite reverse;
    }
    @keyframes float {
        0%, 100% { transform: translate(0, 0); }
        50% { transform: translate(40px, -40px); }
    }
    .hero-inner {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 32px;
        display: grid;
        grid-template-columns: 1.1fr 0.9fr;
        gap: 80px;
        align-items: center;
        position: relative;
        z-index: 1;
    }
    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 8px 18px;
        background: rgba(59,130,246,0.1);
        border: 1px solid rgba(59,130,246,0.3);
        border-radius: 50px;
        font-size: 13px;
        color: #a5c8ff;
        margin-bottom: 28px;
    }
    .hero-badge .dot {
        width: 8px; height: 8px;
        background: #22c55e;
        border-radius: 50%;
        box-shadow: 0 0 0 0 rgba(34,197,94,0.6);
        animation: pulse-dot 2s infinite;
    }
    @keyframes pulse-dot {
        0% { box-shadow: 0 0 0 0 rgba(34,197,94,0.6); }
        70% { box-shadow: 0 0 0 10px rgba(34,197,94,0); }
        100% { box-shadow: 0 0 0 0 rgba(34,197,94,0); }
    }
    .hero-title {
        font-size: 68px;
        font-weight: 900;
        line-height: 1.05;
        letter-spacing: -2px;
        margin-bottom: 24px;
        background: linear-gradient(135deg, #fff 0%, #a5c8ff 50%, #8b5cf6 100%);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        min-height: 1.1em;
    }
    .hero-sub {
        font-size: 18px;
        color: #8a9aa8;
        margin-bottom: 40px;
        max-width: 540px;
    }
    .hero-sub b { color: #3b82f6; font-weight: 700; }
    .hero-buttons {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
        margin-bottom: 56px;
    }
    .hero-trust {
        display: flex;
        align-items: center;
        gap: 32px;
    }
    .trust-item b {
        font-size: 24px;
        font-weight: 800;
        color: #fff;
        line-height: 1;
        display: block;
    }
    .trust-item span {
        font-size: 12px;
        color: #6a7a8a;
        margin-top: 4px;
        display: block;
    }
    .trust-divider {
        width: 1px; height: 40px;
        background: rgba(255,255,255,0.1);
    }
    .hero-visual { position: relative; perspective: 1000px; }
    .hero-card {
        background: rgba(255,255,255,0.03);
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 40px 80px rgba(0,0,0,0.5);
        transform: rotateY(-8deg) rotateX(4deg);
        transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .hero-card:hover { transform: rotateY(0) rotateX(0); }
    .hero-card-header {
        padding: 12px 16px;
        background: rgba(255,255,255,0.03);
        border-bottom: 1px solid rgba(255,255,255,0.08);
        display: flex;
        align-items: center;
        gap: 16px;
    }
    .dots { display: flex; gap: 6px; }
    .dots span {
        width: 10px; height: 10px;
        border-radius: 50%;
        background: rgba(255,255,255,0.15);
    }
    .dots span:nth-child(1) { background: #ff5f57; }
    .dots span:nth-child(2) { background: #febc2e; }
    .dots span:nth-child(3) { background: #28c840; }
    .hero-card-url {
        font-family: 'JetBrains Mono', monospace;
        font-size: 12px;
        color: #6a7a8a;
    }
    .hero-card-body {
        padding: 32px 24px;
        display: flex;
        flex-direction: column;
        gap: 12px;
    }
    .mock-line {
        height: 10px;
        background: linear-gradient(90deg, rgba(255,255,255,0.1), rgba(255,255,255,0.02));
        border-radius: 4px;
    }
    .mock-line.long { width: 90%; }
    .mock-line.medium { width: 65%; }
    .mock-line.short { width: 40%; }
    .mock-btn {
        width: 120px; height: 36px;
        background: linear-gradient(135deg, #3b82f6, #8b5cf6);
        border-radius: 8px;
        margin-top: 16px;
    }
    .hero-float {
        position: absolute;
        padding: 10px 16px;
        background: rgba(255,255,255,0.05);
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 12px;
        font-size: 13px;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        box-shadow: 0 8px 32px rgba(0,0,0,0.3);
        animation: float-y 4s ease-in-out infinite;
    }
    .hero-float-1 { top: -20px; right: -20px; color: #3b82f6; }
    .hero-float-2 { bottom: -20px; left: -20px; color: #22c55e; animation-delay: 2s; }
    @keyframes float-y {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-12px); }
    }
    @media (max-width: 1024px) {
        .hero-inner { grid-template-columns: 1fr; gap: 60px; }
        .hero-title { font-size: 48px; }
        .hero-visual { max-width: 400px; margin: 0 auto; }
    }
    @media (max-width: 640px) {
        .hero { padding: 130px 0 80px; }
        .hero-title { font-size: 36px; letter-spacing: -1px; }
        .hero-sub { font-size: 16px; }
        .hero-trust { flex-wrap: wrap; gap: 20px; }
        .trust-divider { display: none; }
        .hero-inner { padding: 0 20px; }
    }
</style>

<script>
    const heroTitle = document.getElementById('heroTitle');
    if (heroTitle) {
        const finalText = heroTitle.dataset.scramble || '';
        const chars = '!<>-_\\/[]{}—=+*^?#________';
        let iteration = 0;
        
        function scramble() {
            const scrambled = finalText.split('').map((char, index) => {
                if (index < iteration) return finalText[index];
                if (char === ' ') return ' ';
                return chars[Math.floor(Math.random() * chars.length)];
            }).join('');
            heroTitle!.textContent = scrambled;
            if (iteration >= finalText.length) return;
            iteration += 1 / 3;
            requestAnimationFrame(scramble);
        }
        setTimeout(scramble, 600);
    }
</script>